# jaries23 GitHub Pages site

This folder contains a static site ready to publish to a personal GitHub Pages repository.

## Local structure

- `index.html`: landing page markup
- `styles.css`: site styling

## Publish target

Use the repository name `jaries23.github.io` for a personal site at:

`https://jaries23.github.io/`
